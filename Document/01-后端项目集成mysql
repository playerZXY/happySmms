1. 安装mysql
    cnpm i mysql --save

2. 引入mysql
    const mysql = require('mysql')

3. 创建连接对象
    const connection = mysql.createConnection({
    host: 'localhost', // 数据库地址
    user: 'root', // 用户名
    password: 'root', // 密码
    database: 'smms' // 数据库名
    })

4. 执行连接方法
    connection.connect();
